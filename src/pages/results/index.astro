---
import BaseLayout from '../../layouts/BaseLayout.astro';
import results from '../../data/results.json';

const categories = [...new Set(results.map(r => r.category))];
const categoryLabels = {
  board: 'Board Results',
  central: 'Central Govt',
  rajasthan: 'Rajasthan',
  state: 'State Govt',
  banking: 'Banking',
  railway: 'Railway'
};
---

<BaseLayout
  title="Latest Sarkari Result 2026 | Board & Exam Results | Sarkari Sahayak"
  description="2026 के सभी Latest Sarkari Results — CBSE, RBSE, BPSC, SSC CHSL, RPSC, UPSC, SBI, Railway। Board Exam Result, Competitive Exam Result सब यहाँ।"
  canonical="/results/"
>
  <section class="section">
    <div class="container">
      <div class="section-head center-head">
        <span class="section-label">Sarkari Result 2026</span>
        <h1 class="section-title">Latest Results</h1>
        <p class="section-sub">Board exams, competitive exams, recruitment results — sab ek jagah check karein</p>
      </div>

      <div class="chips-row" id="resultFilters" role="group" aria-label="Result category filters">
        <button type="button" class="chip active" data-filter="all">All<span class="cnt">{results.length}</span></button>
        {categories.map(cat => (
          <button type="button" class="chip" data-filter={cat}>
            {categoryLabels[cat] || cat}
            <span class="cnt">{results.filter(r => r.category === cat).length}</span>
          </button>
        ))}
      </div>

      <div class="guide-list" style="margin-top:var(--sp-xl)" id="resultsList">
        {results.map((result, i) => (
          <a href={`/results/${result.slug}`} class="guide-item" data-category={result.category}>
            <span class="guide-num">{i + 1}</span>
            <div class="guide-body">
              <span class="guide-title">{result.title}</span>
              <div class="guide-tags">
                <span class="gtag gtag-cat">{categoryLabels[result.category] || result.category}</span>
                <span class="gtag gtag-time">{result.date}</span>
                <span class="gtag">{result.organization}</span>
              </div>
            </div>
            <span class="guide-arrow">→</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <script is:inline>
  document.addEventListener('DOMContentLoaded', function() {
    var filterBtns = document.querySelectorAll('#resultFilters .chip');
    var items = document.querySelectorAll('#resultsList .guide-item');

    filterBtns.forEach(function(btn) {
      btn.addEventListener('click', function() {
        filterBtns.forEach(function(b) { b.classList.remove('active'); });
        btn.classList.add('active');
        var f = btn.getAttribute('data-filter');
        items.forEach(function(item) {
          if (f === 'all' || item.getAttribute('data-category') === f) {
            item.style.display = '';
          } else {
            item.style.display = 'none';
          }
        });
      });
    });
  });
  </script>
</BaseLayout>
